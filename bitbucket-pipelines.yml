image: node:10.15.0
  
pipelines:
  default:
  test: 
    - step:
        name: Test
        script:
          - npm install
          - npm test
          - npm snyk
  # branches:
  #   backup: 
  #     - step:
  #         name: Create Backup
  #         script:
  #           - npm install
  #           - node -e "require('./node-tasks').init()"